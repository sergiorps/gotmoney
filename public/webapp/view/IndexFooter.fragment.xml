<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m">
    <Toolbar>
        <ToolbarSpacer />
        <Button text="{i18n>Home.about}" press="onAbout" />
        <ToolbarSpacer />
        <Button text="{i18n>Home.privacy}" press="onPrivacy" />
        <ToolbarSpacer />
        <Button text="{i18n>Home.terms}" press="onTerms" />
        <ToolbarSpacer />
    </Toolbar>
</core:FragmentDefinition>